include "main.h"

/**
 * main - Entry point
 * Return: 0
 */   

char *_strcat(char *dest, char *src)
{
    char src;
    char dest;

    strcat(src, dest);
    _putchar(dest);

    return (0);
}

strcat(src, dest)