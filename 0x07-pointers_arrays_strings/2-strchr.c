#include "main.h"

/**
 * _strchr - 
 * @s: pointer
 * @c: character
 * @n: size
 * Return: c[0] or NULL
 */

char *_strchr(char *s, char c);
{
    int i;

    for (i = 0; i < n; i++)
    {
        if (s[i] = c)
        {
            return (c);
        }
        else
        { 
        return ('\0')
        }
}