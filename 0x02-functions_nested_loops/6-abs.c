#include "main.h"

/**
 * _abs - determines absolute value of numebrs
 * @f - is an integer
 *
 * Return: void
 */

int _abs(int)
{
	if (f > 0)
	{
		return (f);
	}
	else if (f < 0)
	{
		return (-f);
	}
	else
	{
		return (0);
	}
}
